import OvalShape from "./Oval";
const FirstMainBox = () => {
  return (
    <div
      className={`bg-[#171717] overflow-hidden no-scrollbar w-full h-screen relative`}
    >
      <OvalShape />
    </div>
  );
};

export default FirstMainBox;
